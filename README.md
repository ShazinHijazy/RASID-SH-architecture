# RASID-SH Final Prototype (Software-only)

## What it is
A Python-only multi-agent swarm simulator with:
- leader election (distance-aware scoring),
- Bayesian reputation updates,
- distance-based communication quality,
- Streamlit live dashboard.

## How to run
1. Create virtualenv (recommended)
   ```bash
   python -m venv venv
   source venv/bin/activate    # Linux / Mac
   venv\Scripts\activate       # Windows
